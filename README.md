# Thunderbird User Chrome

A Thunderbird user chrome module for more compact calendar items.

## Installation

1. Install `chrome` directory into your active profile directory
2. Change `toolkit.legacyUserProfileCustomizations.stylesheets` to `true` in config editor
3. Reload
